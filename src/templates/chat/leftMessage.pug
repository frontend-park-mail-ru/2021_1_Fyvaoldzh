a.message_read(id=interlocutor.id, href='chat?c=' + interlocutor.id)
  .message__avatar(style='background: url(https://qdaqda.ru/api/v1/avatar/' + interlocutor.id + ') no-repeat center / cover;', href='profile'+id)
  .message__body
    .message__author
      | #{interlocutor.name}
    .message__text
      | #{message.text}
  .message__time
    | #{message.date}
